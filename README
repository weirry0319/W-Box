# 图片批量工具箱 · 使用操作文档

> 一个**纯前端、单文件、本地运行**的网页工具，无需安装、无需联网上传，双击即可在浏览器中使用。
> 集两大实用功能于一体：**🖼️ 批量图片处理（改尺寸 + 透明图层合成）** 与 **📝 批量重命名**。

---

## 一、工具简介

本工具文件为 `图片批量工具箱-整合版.html`，所有图片处理、文件重命名均在**你自己的电脑浏览器内完成**，图片不会上传到任何服务器，隐私安全、处理速度快。界面顶部有两个功能标签，点击即可切换：

| 标签 | 功能 | 典型用途 |
| --- | --- | --- |
| **🖼️ 图片处理** | 批量修改图片尺寸，并可叠加一张透明 PNG 模板（如水印、边框、角标），最后打包成 ZIP 下载 | 电商主图/详情页统一尺寸、批量加水印边框、批量压缩 |
| **📝 批量重命名** | 按规则批量重命名文件/文件夹，支持预览、冲突检测、一键撤销 | 图片按编号命名、查找替换字符、去除文件名中的符号 |

---

## 二、运行环境与使用说明

### 1. 浏览器要求
- **推荐使用 Chrome 或 Edge** 浏览器（内核版本建议 86 以上）。
- 「批量重命名」功能依赖浏览器的 **File System Access API** 本地读写能力，**该 API 目前仅 Chrome / Edge 完整支持**；Firefox、Safari 等浏览器打开重命名功能时会提示"当前浏览器不支持此功能"。
- 「图片处理」功能兼容性较好，主流现代浏览器均可使用。

### 2. 如何打开
- 直接**双击** `图片批量工具箱-整合版.html`，即可在默认浏览器中打开使用。
- 若双击后用记事本打开，请右键该文件 →「打开方式」→ 选择 Chrome / Edge。

### 3. 联网说明
- 页面顶部样式字体与 ZIP 打包组件（JSZip）通过 CDN 加载，**首次打开建议联网**以保证完整效果。
- 图片本身的处理全程在本地进行，**不会上传**。

---

## 三、功能一：🖼️ 批量图片处理（改尺寸 + 合成）

整体流程为三步：**① 导入 → ② 设置 → ③ 导出**，界面顶部有步骤进度提示。工作区分为左、中、右三列。

### 步骤 1：导入图片

- **左列下方"拖入文件夹或底层图片"区域**：
  - 直接把**整个文件夹**或**多张图片**拖进虚线框；
  - 或点击「选择文件夹」（递归读取所有子目录图片）/「选择多文件」。
  - 支持 **JPG、JPEG、PNG、WebP、BMP、GIF** 格式。
  - 可**多次拖入追加**，重复文件会自动跳过。
- **左列上方"顶层模板"区域（可选）**：
  - 上传一张**透明 PNG**（如水印、边框、Logo 角标）作为叠加层。
  - **不上传模板** = 仅做尺寸调整/格式转换，不叠加。
  - 上传后可点「更换」替换。
- 导入后中间列会出现**图片列表**（缩略图 + 尺寸 + 大小 + 状态），点击任意一张可在下方查看**实时预览**。
- 需要重来时点列表右上角「清空」。

### 步骤 2：右侧"处理设置"

> 至少**启用尺寸调整**或**上传顶层模板**二者之一，「开始批量处理」按钮才可点击。

**A. 尺寸调整（开关，默认关闭）**
打开"启用尺寸调整"开关后可选三种缩放模式：

| 模式 | 说明 |
| --- | --- |
| **按像素** | 指定目标宽/高（默认 1440px）。可勾选「保持宽高比」避免变形；提供 800×800、800×1200、1440×1920 快捷尺寸按钮 |
| **百分比** | 按原图比例缩放，默认 50%，范围 1%~500% |
| **最长边** | 将图片较长的一边限制为指定像素（默认 1920px），短边等比缩放，常用于批量限制最大尺寸 |

**B. 顶层合成（需上传模板）**

| 设置项 | 说明 |
| --- | --- |
| **顶层缩放** | `原始`（模板按自身像素叠加）/ `宽铺满`（按画布宽铺满）/ `高铺满` / `拉伸`（强制铺满画布） |
| **对齐位置** | 九宫格选择模板在画布上的位置（左上、居中、右下等）。选"拉伸"时此项无意义 |
| **偏移微调（X / Y）** | 在所选对齐基础上做像素级微调 |
| **不透明度** | 模板叠加的透明度，范围 10%~100%（默认 100% 完全不透明） |

**C. 输出**

| 设置项 | 说明 |
| --- | --- |
| **格式** | `保持原格式` / `PNG` / `JPEG` / `WebP`。改格式时文件扩展名会同步变更（GIF 会转为 PNG） |
| **质量** | 仅当格式为 JPEG / WebP 时出现，范围 10%~100%（默认 90%）。数值越高画质越好、体积越大 |

### 步骤 3：导出

1. 确认右侧预览效果满意后，点击底部**「开始批量处理」**。
2. 进度条显示处理进度，列表中每张图状态从"待处理"变为"完成/失败"。
3. 完成后点击**「下载 ZIP」**，打包下载所有处理后的图片。
   - ZIP 文件名自动命名为 `文件夹名_日期_时间.zip`，并**保留原目录层级结构**。

> **小提示**：列表最多预览前 200 张缩略图，超出部分仍会正常处理并打包。

---

## 四、功能二：📝 批量重命名

流程：**选择文件夹 → 设置规则 → 预览 → 执行（可撤销）**。

### 步骤 1：选择目标文件夹
点击「选择文件夹…」并授权读写权限。所选文件夹路径会显示在右侧，工具会通过浏览器本地存储**记住并在下次自动恢复**上次文件夹。

> ⚠ 该功能需 **Chrome / Edge** 浏览器，且需在授权弹窗中点击"允许"。

### 步骤 2：设置命名规则（两种模式二选一）

**模式 A：编号命名**
- **前缀**：如"主图"，留空则直接 1.jpg、2.jpg…；提供"主图/详情页/sku/视频/白底图"快捷标签。
- **起始编号 / 编号位数 / 后缀**：如前缀"图"+起始 1+位数 2 → `图01.jpg`、`图02.jpg`…

**模式 B：查找替换 / 删除**
- **查找内容** + **替换为**（替换为留空 = 删除该字符）。
- 可勾选"区分大小写"。
- 示例：查找"-"替换为"+" → `文件-01.jpg` 变 `文件+01.jpg`；替换为留空 → `文件01.jpg`。

**通用选项：**
- **作用对象**：文件和文件夹 / 仅文件 / 仅文件夹。
- **保留原扩展名**（默认勾选，建议保持）。
- **排序依据**：名称 A→Z / Z→A / 修改时间 新→旧 / 旧→新（决定编号顺序）。

### 步骤 3：预览
表格实时显示「原文件名 → 新文件名」。
- **红色行** = 存在冲突（目标已存在 或 批次内重名），执行时会被**自动跳过**，防止覆盖。
- 状态栏汇总"共 N 项 / 需改名 N 项 / 冲突 N 项"。

### 步骤 4：执行与撤销
- 点击**「执行重命名」**批量改名，下方黑色日志区实时打印处理记录。
- 点**「撤销上次」**可一键回滚本次改名（恢复原名）。
- **快捷键**：
  - `回车` = 执行重命名
  - `Backspace` = 撤销上次
  - `/`、`\` 或 `、` = 刷新预览
  - （光标在输入框内时快捷键不触发，避免误操作）

---

## 五、常见问题（FAQ）

**Q1：双击文件没有用浏览器打开？**
右键文件 →「打开方式」→ 选择 Chrome / Edge。

**Q2：批量重命名提示"当前浏览器不支持"？**
请改用 **Chrome 或 Edge** 浏览器。图片处理功能对浏览器要求较宽松。

**Q3：图片会泄露/上传吗？**
不会。所有处理都在浏览器本地完成，图片不出本机。仅 ZIP 组件与字体走 CDN 加载。

**Q4：改完尺寸图片变形了？**
"按像素"模式下请勾选**「保持宽高比」**，或改用"百分比 / 最长边"模式（二者自动等比缩放）。

**Q5：重命名怕改错？**
先用"预览"核对，红色冲突行会被跳过；执行后随时可点「撤销上次」恢复。建议对重要文件夹先备份。

**Q6：导出的图片文件名/目录结构乱了吗？**
不会。ZIP 会按导入时的相对路径保留原始目录层级；改输出格式时仅扩展名同步变更。

---

## 六、技术说明（可选阅读）

- **单文件交付**：全部功能集成在一个 `.html` 文件内，便于分发与归档。
- **本地能力**：重命名基于 File System Access API（`showDirectoryPicker`）+ IndexedDB 记忆文件夹句柄；图片处理基于 Canvas 2D 渲染；打包基于 JSZip。
- **兼容性提示**：File System Access API 为 Chromium 系浏览器特性，跨浏览器部署时重命名模块存在局限。

---

*本工具为纯前端离线工具，请放心在本地处理涉及隐私的图片与文件。*
